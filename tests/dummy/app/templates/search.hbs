<h1>Search Results</h1>

<div id="posts">
  {{#each model as |post|}}
    <div id="post-{{post.id}}" class="post">
      {{#link-to 'post' post.slug}}{{post.title}}{{/link-to}}
    </div>
  {{/each}}
</div>
